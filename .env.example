# Copy to .env and fill in. Never commit .env.
DATABASE_URL="postgresql://bindr:bindr@localhost:5433/bindr?schema=public"

# `openssl rand -base64 32`
BETTER_AUTH_SECRET=""
BETTER_AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional — raises pokemontcg.io rate limits (https://dev.pokemontcg.io)
POKEMONTCG_API_KEY=""

# Google OAuth (later)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Stripe (later — subscription + one-off fulfillment charges)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# AI assembly agent (later)
ANTHROPIC_API_KEY=""

# Cache / rate-limiting (later)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
